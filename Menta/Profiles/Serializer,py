# serializers.py
from rest_framework.serializers import ModelSerializer

from .models import Profile

class ProfileSerializer(ModelSerializer):
    class Meta:
        model = Profile
        fields = (
            'id', 'name', 'TYPE_CHOICES', 'userType', 'occupation', 'age',
            'GENDER_CHOICES', 'gender', 'biography', 'photos'
        )